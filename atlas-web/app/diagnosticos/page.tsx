import DiagnosticosPage from "../../components/DiagnosticosPage";

import { diagnosticos } from "../../data/diagnosticos";

export default function Diagnosticos() {
  return <DiagnosticosPage diagnosticos={diagnosticos} />;
}
